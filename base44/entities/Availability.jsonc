{
  "name": "Availability",
  "type": "object",
  "properties": {
    "day_of_week": {
      "type": "number",
      "title": "Día de la semana (0=domingo, 6=sábado)"
    },
    "start_time": {
      "type": "string",
      "title": "Hora de inicio (HH:mm)"
    },
    "end_time": {
      "type": "string",
      "title": "Hora de fin (HH:mm)"
    },
    "type": {
      "type": "string",
      "title": "Tipo",
      "enum": [
        "work",
        "break",
        "holiday",
        "block"
      ],
      "default": "work"
    },
    "date": {
      "type": "string",
      "format": "date",
      "title": "Fecha específica (para feriados/bloqueos)"
    },
    "label": {
      "type": "string",
      "title": "Etiqueta (ej. Feriado, Almuerzo)"
    },
    "professional_ref_id": {
      "type": "string",
      "title": "Profesional (vacio = horario del dueno de la cuenta)"
    },
    "practice_owner_id": {
      "type": "string",
      "title": "Consultorio al que pertenece este horario"
    }
  },
  "required": [
    "day_of_week",
    "start_time",
    "end_time",
    "type"
  ],
  "rls": {
    "read": {},
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}